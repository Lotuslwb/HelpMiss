<!--index.wxml-->
<!-- <view class="index-page">
</view> -->

<view class="display-info">
        <view class="title">{{title[queryType]}}</view>
        <block wx:for="{{infos}}" wx:key="{{item._id}}">
            <findInfo info-id = "{{item._id}}" id = "{{item._id}}" 
                    info = "{{item}}"
                    />
        </block>
</view>